import React from 'react'

const ToolUsed = ({label}) => {
  return (
    <div className='bg-blue-500 text-center px-2 sm:px-3 rounded-full font-kanit font-light text max-sm:text-sm text-white' >
        {label}
    </div>
  )
}

export default ToolUsed
